import { ComponentStyleConfig } from "@chakra-ui/react";

const Link: ComponentStyleConfig = {
  baseStyle: {
    textDecoration: "none",
    _hover: {
      textDecoration: "none",
    },
  },
};

export default Link